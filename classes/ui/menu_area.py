from enum import Enum, auto

class MenuArea(Enum):
    MAIN_MENU = auto()
    SIMULATION = auto()
    OPTIONS = auto()
    CATALOGUE = auto()
    CREDITS = auto()
    DEBUG = auto()
    FEATURE_LIST = auto()